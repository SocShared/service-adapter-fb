server:
  port: 8081
  error:
    whitelabel:
      enabled: false
spring:
  application:
    name: socshared-adapter-fb
  cloud:
    kubernetes:
      config:
        enabled: false
        name: ${spring.application.name}-config
      reload:
        enabled: false
keycloak:
  enabled: false
